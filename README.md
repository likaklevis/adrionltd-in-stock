# Adrionltd In-Stock Filter

Browser extension that automatically keeps the in-stock filter enabled on adrionltd.com by updating the URL hash.

```text
URL hash → Base64 decode → JSON object → set stock = 1 → encode again → update URL
```

## Setup

- Firefox: install from https://addons.mozilla.org/en-US/firefox/addon/adrionltd-in-stock/
- Chrome / Chromium / Edge: download, unzip, and load it as an unpacked extension.

Navigate to [adrionltd.com](https://adrionltd.com). Filtering is automatic.
Not affiliated with Adrion Ltd.
